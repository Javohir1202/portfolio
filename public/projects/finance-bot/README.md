Drop real Finance Bot (Telegram) screenshots here with these exact filenames (referenced in `data/projects.ts`), ideally cropped to just the chat area:

- `start.png` — the /start welcome message + account-linking flow ("С возвращением! Аккаунт уже привязан." or the 6-digit code message)
- `income.png` — an added-income confirmation (e.g. "✅ Доход добавлен")
- `expense.png` — the expense flow: typing an amount and the bot's "✅ Расход добавлен" confirmation
- `menu.png` — the reply-keyboard menu (💰 Доход / 💸 Расход / 📊 Статистика / etc.)
- `today.png` — a "📅 Сегодня" stats reply

These render inside a phone-shaped frame, so a portrait screenshot works best. Until a file exists, that slot renders a clearly-labelled placeholder instead of a broken or fake image.
