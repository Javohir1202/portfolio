Drop real ServiceFlow screenshots here with these exact filenames (referenced in `data/projects.ts`):

- `dashboard.png` — the private dashboard (KPIs, lead funnel, overdue tasks)
- `pipeline.png` — the drag-and-drop lead pipeline board (Новый / Связались / Квалифицирован / Смета отправлена / Выигран / Проигран)
- `customers.png` — the customers list/detail view
- `jobs.png` — the Orders (Заказы) view
- `tasks.png` — the Tasks (Задачи) view
- `appointments.png` — the Appointments (Встречи) view
- `analytics.png` — the Analytics view (conversion, revenue by month, leads by source)

Recommended: 1600px wide, PNG or JPG. Until a file exists, that slot renders a clearly-labelled placeholder frame instead of a broken or fake image.

Note: screenshots of the public *marketing* homepage are intentionally not used here — that page's own hero/pipeline imagery is AI-generated mockup content with fabricated data, not the real product. Only the authenticated app's real screens (even empty-state ones) go in this folder.
