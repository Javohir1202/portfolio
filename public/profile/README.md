Drop the profile photo here as `javohir.jpg` (or update the path in `data/site.ts` → `photo.src`).

Until a file exists at this path, the hero section shows an initials placeholder instead of a broken image — nothing to configure, it just works once the file is added.
